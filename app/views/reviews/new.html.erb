<h1>Write a New Item For <%=link_to @item.name, "/merchants/#{@item.id}" %></h1>

<center>
  <%= form_tag "/items/#{@item.id}", method: :post do %>
    <%= text_field_tag :title, "Title"%>

    <%= text_field_tag :content, "Review" %>

    <%= text_field_tag :rating, "Rating (Out of 5 Star)" %>

    <%= submit_tag 'Save Review' %>
  <% end %>
</center>
